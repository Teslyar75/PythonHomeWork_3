# 📚 Детальна документація проекту "Облік витрат"

## 🎯 Мета документації

Цей документ допоможе вам, як студенту, детально розібратися в архітектурі та роботі застосунку для обліку витрат, реалізованого з використанням патерну MVC (Model-View-Controller).

---

## 📖 Зміст

1. [Що таке патерн MVC?](#що-таке-патерн-mvc)
2. [Архітектура проекту](#архітектура-проекту)
3. [Детальний розбір компонентів](#детальний-розбір-компонентів)
4. [Як працює застосунок?](#як-працює-застосунок)
5. [Потік виконання](#потік-виконання)
6. [Приклади використання](#приклади-використання)
7. [Питання та відповіді](#питання-та-відповіді)

---

## 🏗️ Що таке патерн MVC?

**MVC (Model-View-Controller)** - це архітектурний патерн, який розділяє застосунок на три основні компоненти:

### 1. **Model (Модель)** - Дані та бізнес-логіка
- Відповідає за роботу з даними
- Зберігає та обробляє інформацію
- Не знає про існування View та Controller
- У нашому проекті: `model.py`

### 2. **View (Представлення)** - Інтерфейс користувача
- Відповідає за відображення інформації
- Отримує введення від користувача
- Не знає про бізнес-логіку
- У нашому проекті: `view.py`

### 3. **Controller (Контролер)** - Посередник
- З'єднує Model та View
- Обробляє запити користувача
- Приймає рішення про те, що робити далі
- У нашому проекті: `controller.py`

### 🎨 Візуалізація взаємодії:

```
┌─────────────┐
│   Користувач │
└──────┬───────┘
       │
       ▼
┌─────────────┐
│    VIEW     │ ◄─── Відображає інформацію
│  (view.py)  │      Отримує введення
└──────┬───────┘
       │
       ▼
┌─────────────┐
│ CONTROLLER  │ ◄─── Обробляє запити
│(controller) │      Приймає рішення
└──────┬───────┘
       │
       ▼
┌─────────────┐
│    MODEL    │ ◄─── Працює з даними
│ (model.py)  │      Зберігає/завантажує
└─────────────┘
```

---

## 📁 Архітектура проекту

### Структура файлів:

```
HomeWork_3/
├── model.py          # MODEL - робота з даними
├── view.py           # VIEW - інтерфейс користувача
├── controller.py     # CONTROLLER - логіка застосунку
├── main.py           # Точка входу
├── README.md         # Короткий опис
└── .gitignore        # Файли для ігнорування Git
```

### Залежності між модулями:

```
main.py
  │
  ├──► імпортує model.py
  ├──► імпортує view.py
  └──► імпортує controller.py

controller.py
  │
  ├──► імпортує model.py
  └──► імпортує view.py

view.py
  │
  └──► імпортує model.py (тільки для типізації)
```

---

## 🔍 Детальний розбір компонентів

### 1. MODEL (model.py)

#### Клас `Expense` - Представлення однієї витрати

**Призначення:** Цей клас представляє одну витрату як об'єкт.

**Атрибути:**
- `id` - унікальний ідентифікатор витрати
- `amount` - сума витрати (float)
- `description` - опис витрати (str)
- `category` - категорія витрати (str)
- `date` - дата та час створення (str)

**Методи:**
- `to_dict()` - перетворює об'єкт витрати в словник (для збереження в JSON)
- `from_dict()` - створює об'єкт витрати зі словника (для завантаження з JSON)

**Приклад використання:**
```python
# Створення витрати
expense = Expense(expense_id=1, amount=100.50, description="Продукти", category="Їжа")

# Перетворення в словник для збереження
expense_dict = expense.to_dict()
# Результат: {'id': 1, 'amount': 100.50, 'description': 'Продукти', ...}

# Створення зі словника
expense_from_dict = Expense.from_dict(expense_dict)
```

#### Клас `ExpenseModel` - Модель для роботи з даними

**Призначення:** Цей клас відповідає за всі операції з даними витрат.

**Атрибути:**
- `storage_file` - шлях до файлу для збереження (за замовчуванням "expenses.json")
- `expenses` - список всіх витрат (List[Expense])
- `next_id` - наступний доступний ID для нової витрати

**Основні методи:**

##### `add_expense(amount, description, category)`
**Що робить:** Додає нову витрату до списку
**Як працює:**
1. Створює новий об'єкт `Expense` з унікальним ID
2. Додає його до списку `self.expenses`
3. Збільшує `next_id` для наступної витрати
4. Автоматично зберігає дані у файл
5. Повертає створену витрату

**Приклад:**
```python
model = ExpenseModel()
expense = model.add_expense(amount=50.0, description="Кава", category="Напої")
# expense.id буде 1 (якщо це перша витрата)
```

##### `delete_expense(expense_id)`
**Що робить:** Видаляє витрату за її ID
**Як працює:**
1. Шукає витрату з вказаним ID у списку
2. Якщо знайдена - видаляє її
3. Автоматично зберігає зміни у файл
4. Повертає `True` якщо видалено, `False` якщо не знайдено

**Приклад:**
```python
success = model.delete_expense(expense_id=1)
if success:
    print("Витрата видалена!")
```

##### `get_all_expenses()`
**Що робить:** Повертає копію списку всіх витрат
**Примітка:** Повертає копію, щоб не можна було змінити оригінальний список ззовні

**Приклад:**
```python
all_expenses = model.get_all_expenses()
for expense in all_expenses:
    print(f"{expense.id}: {expense.amount} грн")
```

##### `get_total_amount()`
**Що робить:** Обчислює загальну суму всіх витрат
**Як працює:** Проходить по всіх витратах і додає їх суми

**Приклад:**
```python
total = model.get_total_amount()
print(f"Загальна сума: {total} грн")
```

##### `save_expenses()` та `load_expenses()`
**Що роблять:** Зберігають та завантажують дані з JSON файлу
**Як працюють:**
- `save_expenses()` - перетворює всі витрати в словники та зберігає в JSON
- `load_expenses()` - читає JSON файл та відновлює об'єкти витрат

**Формат JSON файлу:**
```json
[
  {
    "id": 1,
    "amount": 100.5,
    "description": "Продукти",
    "category": "Їжа",
    "date": "2024-01-15 10:30:00"
  },
  {
    "id": 2,
    "amount": 50.0,
    "description": "Кава",
    "category": "Напої",
    "date": "2024-01-15 11:00:00"
  }
]
```

---

### 2. VIEW (view.py)

#### Клас `ExpenseView` - Інтерфейс користувача

**Призначення:** Відповідає за всі взаємодії з користувачем - відображення інформації та отримання введення.

**Всі методи статичні** - це означає, що вони не потребують створення екземпляру класу.

**Методи для отримання введення:**

##### `get_user_choice()`
**Що робить:** Отримує вибір користувача з меню
**Повертає:** Рядок з номером опції (1-5)

##### `get_expense_input()`
**Що робить:** Отримує дані про нову витрату від користувача
**Повертає:** Кортеж (amount, description, category)
**Обробка помилок:** Якщо користувач ввів некоректну суму, повертає (None, None, None)

**Приклад використання:**
```python
amount, description, category = ExpenseView.get_expense_input()
# Користувач вводить:
# Сума: 100
# Опис: Продукти
# Категорія: Їжа
# Результат: (100.0, "Продукти", "Їжа")
```

##### `get_expense_id()`
**Що робить:** Отримує ID витрати для видалення
**Повертає:** Ціле число або None якщо помилка

**Методи для відображення:**

##### `show_menu()`
**Що робить:** Показує головне меню застосунку
**Вивід:**
```
==================================================
📊 ОБЛІК ВИТРАТ
==================================================
1. Додати нову витрату
2. Видалити витрату за id
3. Показати список витрат
4. Показати загальну суму витрат
5. Вийти
==================================================
```

##### `show_expense_added(expense)`
**Що робить:** Показує повідомлення про успішне додавання витрати
**Вивід:**
```
✅ Витрата додана успішно!
   ID: 1
   Сума: 100.0 грн
   Опис: Продукти
   Категорія: Їжа
   Дата: 2024-01-15 10:30:00
```

##### `show_expenses_list(expenses)`
**Що робить:** Відображає список всіх витрат у вигляді таблиці
**Як працює:**
1. Перевіряє, чи є витрати
2. Якщо немає - показує повідомлення
3. Якщо є - форматує їх у таблицю

**Приклад виводу:**
```
================================================================================
📋 СПИСОК ВИТРАТ
================================================================================
ID    Сума         Категорія      Опис                           Дата
--------------------------------------------------------------------------------
1     100.50       Їжа            Продукти                       2024-01-15 10:30:00
2     50.00        Напої          Кава                           2024-01-15 11:00:00
================================================================================
Всього витрат: 2
```

##### `show_total_amount(total)`
**Що робить:** Показує загальну суму витрат
**Вивід:**
```
==================================================
💰 ЗАГАЛЬНА СУМА ВИТРАТ: 150.50 грн
==================================================
```

##### `show_error(message)` та `show_message(message)`
**Що роблять:** Показують повідомлення про помилки та загальні повідомлення

---

### 3. CONTROLLER (controller.py)

#### Клас `ExpenseController` - Логіка застосунку

**Призначення:** Контролер з'єднує Model та View, обробляє логіку застосунку.

**Атрибути:**
- `model` - екземпляр класу `ExpenseModel`
- `view` - екземпляр класу `ExpenseView`

**Методи:**

##### `add_expense()`
**Що робить:** Обробляє процес додавання нової витрати
**Як працює:**
1. Отримує дані від користувача через `view.get_expense_input()`
2. Перевіряє, чи дані коректні (amount не None)
3. Перевіряє, чи сума більше нуля
4. Якщо все ОК - викликає `model.add_expense()`
5. Показує результат через `view.show_expense_added()`

**Потік виконання:**
```
Користувач → View (введення) → Controller (перевірка) → Model (збереження) → View (відображення)
```

##### `delete_expense()`
**Що робить:** Обробляє видалення витрати
**Як працює:**
1. Отримує ID від користувача через `view.get_expense_id()`
2. Викликає `model.delete_expense()`
3. Показує результат через `view.show_expense_deleted()`

##### `show_expenses_list()`
**Що робить:** Показує список витрат
**Як працює:**
1. Отримує список витрат через `model.get_all_expenses()`
2. Передає його в `view.show_expenses_list()` для відображення

##### `show_total_amount()`
**Що робить:** Показує загальну суму
**Як працює:**
1. Отримує загальну суму через `model.get_total_amount()`
2. Передає її в `view.show_total_amount()` для відображення

##### `run()` - Головний цикл застосунку
**Що робить:** Запускає інтерактивний цикл застосунку
**Як працює:**
1. Показує меню через `view.show_menu()`
2. Отримує вибір користувача через `view.get_user_choice()`
3. Залежно від вибору викликає відповідний метод:
   - '1' → `add_expense()`
   - '2' → `delete_expense()`
   - '3' → `show_expenses_list()`
   - '4' → `show_total_amount()`
   - '5' → вихід з циклу
4. Після кожної операції чекає натискання Enter
5. Повторює цикл до вибору опції "5"

---

### 4. MAIN (main.py)

**Призначення:** Точка входу застосунку - файл, який запускається першим.

**Що робить:**
1. Імпортує необхідні класи
2. Створює екземпляри Model, View та Controller
3. Запускає застосунок через `controller.run()`

**Код:**
```python
def main():
    # Створюємо компоненти MVC
    model = ExpenseModel()      # Модель даних
    view = ExpenseView()        # Представлення
    controller = ExpenseController(model, view)  # Контролер
    
    # Запускаємо застосунок
    controller.run()

if __name__ == '__main__':
    main()
```

**Чому `if __name__ == '__main__':`?**
Це перевірка, що файл запущено напряму, а не імпортовано як модуль. Це дозволяє використовувати файл як скрипт та як модуль.

---

## 🔄 Як працює застосунок?

### Загальний потік:

1. **Запуск** (`main.py`)
   ```
   python main.py
   ```

2. **Ініціалізація**
   - Створюється `ExpenseModel` - завантажує дані з `expenses.json` (якщо файл існує)
   - Створюється `ExpenseView` - готовий до відображення
   - Створюється `ExpenseController` - отримує посилання на model та view

3. **Головний цикл** (`controller.run()`)
   - Показує меню
   - Очікує вибір користувача
   - Виконує відповідну дію
   - Повторює цикл

4. **Збереження даних**
   - Після кожної зміни (додавання/видалення) дані автоматично зберігаються в `expenses.json`

---

## 📊 Потік виконання на прикладах

### Приклад 1: Додавання витрати

```
1. Користувач обирає опцію "1"
   ↓
2. Controller.add_expense() викликається
   ↓
3. View.get_expense_input() запитує дані:
   - Сума: 100
   - Опис: Продукти
   - Категорія: Їжа
   ↓
4. Controller перевіряє дані:
   - amount не None? ✅
   - amount > 0? ✅
   ↓
5. Model.add_expense() викликається:
   - Створюється Expense(id=1, amount=100, ...)
   - Додається до списку
   - Зберігається в expenses.json
   ↓
6. View.show_expense_added() показує результат:
   ✅ Витрата додана успішно!
```

### Приклад 2: Перегляд списку витрат

```
1. Користувач обирає опцію "3"
   ↓
2. Controller.show_expenses_list() викликається
   ↓
3. Model.get_all_expenses() повертає список витрат
   ↓
4. View.show_expenses_list() форматує та відображає таблицю
```

### Приклад 3: Видалення витрати

```
1. Користувач обирає опцію "2"
   ↓
2. Controller.delete_expense() викликається
   ↓
3. View.get_expense_id() запитує ID: 1
   ↓
4. Model.delete_expense(1) шукає витрату:
   - Знаходить витрату з id=1
   - Видаляє її зі списку
   - Зберігає зміни в expenses.json
   - Повертає True
   ↓
5. View.show_expense_deleted(1, True) показує:
   ✅ Витрата з id=1 успішно видалена!
```

---

## 💡 Приклади використання

### Сценарій 1: Перший запуск

```
Користувач запускає: python main.py

1. Створюється expenses.json (порожній)
2. Показується меню
3. Користувач обирає "1" - додати витрату
4. Вводить: 100, "Продукти", "Їжа"
5. Витрата зберігається
6. Файл expenses.json створюється з даними
```

### Сценарій 2: Робота з існуючими даними

```
Користувач запускає: python main.py

1. Model завантажує дані з expenses.json
2. Показується меню
3. Користувач обирає "3" - список витрат
4. Відображаються всі збережені витрати
```

---

## ❓ Питання та відповіді

### Чому використовується патерн MVC?

**Відповідь:** MVC дозволяє:
- **Розділити відповідальність** - кожен компонент має свою роль
- **Легше тестувати** - можна тестувати кожен компонент окремо
- **Легше підтримувати** - зміни в одному компоненті не впливають на інші
- **Легше розширювати** - можна додати новий View без зміни Model

### Чому Model не знає про View?

**Відповідь:** Це принцип **розділення відповідальності**. Model відповідає тільки за дані, а не за те, як вони відображаються. Це дозволяє використовувати ту саму Model з різними View (наприклад, консольний інтерфейс, веб-інтерфейс, GUI).

### Чому використовується JSON для збереження?

**Відповідь:** JSON - це простий формат для зберігання структурованих даних. Він:
- Легко читається людьми
- Легко парситься програмами
- Підтримується багатьма мовами програмування
- Для простих проектів достатньо (для складніших можна використовувати SQLite або інші БД)

### Як працює автоматичне збереження?

**Відповідь:** Після кожної операції, що змінює дані (додавання/видалення), викликається `save_expenses()`, який:
1. Перетворює всі об'єкти Expense в словники
2. Записує їх у JSON файл
3. Якщо файл не існує - створює його

### Що станеться, якщо файл expenses.json пошкоджений?

**Відповідь:** Метод `load_expenses()` обгортає читання в `try-except`. Якщо виникає помилка:
- Виводиться повідомлення про помилку
- Створюється порожній список витрат
- Застосунок продовжує працювати з порожнім списком

### Чому використовуються статичні методи у View?

**Відповідь:** Статичні методи не потребують створення екземпляру класу. Оскільки View не зберігає стан (дані), всі методи можуть бути статичними. Це спрощує код та робить його більш функціональним.

---

## 🎓 Висновки

### Що ви дізналися:

1. **Архітектура MVC** - як розділити застосунок на компоненти
2. **Робота з даними** - збереження/завантаження з JSON
3. **Інтерактивний інтерфейс** - робота з користувачем через консоль
4. **Обробка помилок** - валідація введення
5. **Розділення відповідальності** - кожен клас має свою роль

### Наступні кроки для розвитку:

1. **Додати редагування витрат** - можливість змінити існуючу витрату
2. **Фільтрація** - показувати витрати за категорією або датою
3. **Статистика** - графіки, середні значення
4. **Експорт** - збереження в Excel або PDF
5. **GUI інтерфейс** - замість консолі використати Tkinter або PyQt

---

## 📝 Додаткові матеріали для вивчення

- **Патерни проектування** - MVC, Singleton, Factory
- **Робота з файлами** - JSON, CSV, XML
- **Обробка винятків** - try-except, валідація даних
- **ООП в Python** - класи, наслідування, інкапсуляція
- **Тестування** - unit tests для кожного компонента

---

**Успіхів у навчанні! 🚀**

*Якщо у вас виникли питання - не соромтеся їх задавати!*

