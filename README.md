# Застосунок для обліку витрат

Застосунок для ведення обліку витрат з реалізацією патерну MVC (Model-View-Controller).

## 📋 Опис

Застосунок дозволяє вести облік витрат з наступним функціоналом:
- ✅ Додавання нової витрати
- ✅ Видалення існуючої витрати за id
- ✅ Запит списку витрат
- ✅ Виведення загальної суми витрат

## 🏗️ Структура проекту

Проект реалізований з використанням патерну MVC:

```
HomeWork_3/
├── model.py       # MODEL - робота з даними витрат
├── view.py        # VIEW - інтерфейс користувача
├── controller.py  # CONTROLLER - логіка застосунку
└── main.py        # Точка входу для запуску
```

### Компоненти:

- **Model** (`model.py`) - класи `Expense` та `ExpenseModel` для роботи з даними
- **View** (`view.py`) - клас `ExpenseView` для відображення інформації
- **Controller** (`controller.py`) - клас `ExpenseController` для обробки логіки
- **Main** (`main.py`) - головний файл для запуску застосунку

## 🚀 Запуск

```bash
python main.py
```

## 💾 Зберігання даних

Дані автоматично зберігаються у файл `expenses.json` у форматі JSON.

## 📝 Приклад використання

1. Запустіть застосунок: `python main.py`
2. Оберіть опцію з меню:
   - `1` - Додати нову витрату
   - `2` - Видалити витрату за id
   - `3` - Показати список витрат
   - `4` - Показати загальну суму витрат
   - `5` - Вийти

## 🛠️ Технології

- Python 3.x
- JSON для зберігання даних
- Патерн MVC для архітектури

## 📄 Ліцензія

Цей проект створено в навчальних цілях.

